# Security

This repository is intended to stay static and public.

Rules:
- Do not commit secrets
- Do not add production API keys to front-end code
- Keep sensitive processing pipelines in separate private repositories
- Treat this repo as a publishable shell, not as a confidential workspace
